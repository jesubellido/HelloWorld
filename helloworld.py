import sys
import os
lista[]
x = input("Cómo te llamas?")
for i in x:
	int contador=0
	if lista.count(i)=0:
		for j in x:
			if i==j:
				contador=contador+1
		lista.append(i)
		print(f"{i} = {contador}")
